{{- if .Values.loki.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-storage
  namespace: {{ .Release.Namespace }}
  labels:
    app: loki
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.global.storage.storageClass }}
  resources:
    requests:
      storage: {{ .Values.global.storage.sizes.medium }}
{{- end }}


